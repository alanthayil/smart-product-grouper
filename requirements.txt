# Phase 1 — minimal deps; expand as pipeline is implemented
pytest>=7.0.0
ruff>=0.1.0
pandas>=2.0.0
openpyxl>=3.0.0
openai>=1.0.0

# Placeholders for vertical slice (uncomment when needed)
# scikit-learn>=1.3.0
